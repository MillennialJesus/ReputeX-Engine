// Every script needs a modifier function
const modifier = (text) => {
  text = AutoCards("output", text);  //-- ⚠ Only call if using AutoCards!! ⚠ --//
  // Directly call your global function
  return outputModifier(text);
};
// Don't modify this part
modifier(text)
