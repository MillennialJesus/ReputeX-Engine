// Every script needs a modifier function
const modifier = (text) => {
  text = AutoCards("input", text);  //-- ⚠ Only call if using AutoCards!! ⚠ --//
  // Directly call your global function
  return inputModifier(text);
};
// Don't modify this part
modifier(text)
